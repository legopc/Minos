# Zones Tab

The **Zones** tab displays zone master controls as colour-coded cards, each representing one zone (group of output channels).

## Zone Cards

Each zone card shows:

- **Zone name** (e.g., "Main Outs", "Mic Mix").
- **Zone colour** assigned in config (0–9 palette).
- **Output fader** for each TX channel in the zone: Adjust master level or individual channel volume.
- **Mute button** per output: Silence specific channels in the zone.
- **DSP icon** per output: Open the DSP panel to edit output DSP (EQ, limiter, delay, etc.).
- **Meter** per output: Real-time level display.

## Common Tasks

- **Adjust zone volume**: Move the zone master fader up/down.
- **Mute a zone**: Click the mute button.
- **Edit output DSP**: Click the DSP icon to open the parametric EQ, limiter, delay, or other processors.
- **Link stereo pairs**: If configured in `stereo_links`, pan controls appear here (future feature).

## Colour Organization

Zone colours match the routing matrix and mixer tab for consistent visual reference across the entire interface.
