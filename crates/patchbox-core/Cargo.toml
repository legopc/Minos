[package]
name = "patchbox-core"
version = "0.1.0"
edition = "2021"

[dependencies]
aec3 = { version = "0.1.7", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
serde_json = "1"

[features]
default = []
aec = ["dep:aec3"]
