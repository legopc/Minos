# Playwright UI smoke tests — s7-test-ui-smoke
# Run: `npx playwright test`
# Scenarios:
#   1. Load / — all tabs render
#   2. Switch tabs
#   3. Open DSP panel from a strip badge
#   4. Drag fader — PUT /inputs/:ch volume_db
#   5. Add matrix route — POST /routes
#   6. Scene recall
